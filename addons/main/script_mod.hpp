#define MAINPREFIX x
#define PREFIX tacgtrs
#define SUBPREFIX addons

#include "script_version.hpp"

#ifdef COMPONENT_BEAUTIFIED
    #define COMPONENT_NAME QUOTE(GT Armory Recoil System - COMPONENT_BEAUTIFIED)
#else
    #define COMPONENT_NAME QUOTE(GT Armory Recoil System - COMPONENT)
#endif
